---
"@ipedro/lossless-claude": minor
---

Default summarization to `auto`, which uses `claude-process` for Claude sessions and `codex-process` for Codex sessions, and add the new `codex-process` summarizer provider.
